import 'package:flutter/material.dart';

class ChatAddDialog extends StatelessWidget {
  const ChatAddDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}
